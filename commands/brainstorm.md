---
description: Design a new feature — produces a brainstorming spec that hard-gates implementation until you approve it.
argument-hint: <feature description>
---

Invoke the `zeus:brainstorming` skill via the Skill tool. Pass these arguments to the skill: $ARGUMENTS

If the user did not supply any arguments after the slash command, ask them in one sentence what feature, refactor, or change they want to design before invoking the skill — do not invoke an empty brainstorming session.
