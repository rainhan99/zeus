---
description: Finish the current development branch — verify clean state and observability, then present merge / PR / push / keep options.
---

Invoke the `zeus:finishing-a-development-branch` skill via the Skill tool. No arguments needed.

The skill will only run after G7 closes (session-handoff, clean-state, and observability all pass). Merge actions are NEVER auto-executed — the skill presents options and waits for your explicit choice.
