# Fixture Spec — legacy (no stable IDs)

## Goal / Scope

This spec predates the stable-ID format. It enumerates features only in prose:
the system should support login, logout, and password reset. There are no
line-anchored checklist identifiers anywhere in this document.
